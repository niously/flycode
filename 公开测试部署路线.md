# Flycode 公开测试部署路线

## 结论

当前 Flycode 已经可以作为本机和局域网测试版使用，但不能直接把 `data/db.json` 版本长期暴露到公网。最省成本的路线是：

```text
本机/局域网验证
  -> CloudBase Run 单实例小范围测试
  -> CloudBase 数据库 + 云托管正式上线
```

## 当前版本的限制

- 数据保存在本地 JSON 文件，容器重启、重新部署或实例迁移可能导致数据丢失。
- 管理员使用请求头密钥，不是正式账号系统。
- 只有单实例临时测试适合使用本地文件；不要开启多实例弹性扩容来承载这个版本。
- 当前不支持图片/视频上传，不需要先配置对象存储。
- 当前适合文字提案和低流量验证，不适合大规模公开活动。

## 第一步：本机验收

```bash
cd C:/Users/l2104/flycode
npm test
```

确认看到：

```text
PASS: isolated Flycode smoke checks
```

启动本机服务：

```bash
npm start
```

同一 Wi-Fi 下，手机使用启动日志中的 `http://192.168.x.x:4173` 地址访问。

## 第二步：CloudBase Run 小范围测试

腾讯云 CloudBase Run 支持上传本地代码包、Git 仓库或容器镜像部署 Node.js 服务。当前项目已经包含：

- `Dockerfile`
- `.dockerignore`
- `PORT` 环境变量支持
- `FLYCODE_ADMIN_KEY` 环境变量支持
- `FLYCODE_DATA_DIR` 数据目录支持
- `FLYCODE_RELEASE_ID` 环境变量支持；部署后 `/api/health` 会返回实际构建标识

### 控制台设置

创建 CloudBase Run 服务时：

```text
服务名：flycode
端口：8080
启动命令：使用 Dockerfile 默认 CMD
NODE_ENV：production
FLYCODE_ADMIN_KEY：一段长而随机的密钥
FLYCODE_DATA_DIR：/data
FLYCODE_RELEASE_ID：部署前执行 git rev-parse HEAD 得到的完整 SHA
```

容器会监听 `0.0.0.0:8080`。CloudBase Run 会通过平台入口转发流量。

首次部署时只建议：

- 1 个实例
- 最低可用资源
- 只邀请自己和少量朋友测试
- 不要把默认域名当作正式生产域名
- 部署后先访问 `/api/health`，确认 `release` 与本次填入的 SHA 一致

CloudBase 官方参考：

- [云托管 CloudBase Run](https://cloud.tencent.com/document/product/1243/121989)
- [构建并部署 Node.js](https://cloud.tencent.com/document/product/1243/49237)
- [服务设置](https://cloud.tencent.com/document/product/1243/77197)
- [环境变量](https://docs.cloudbase.net/run/deploy/configuring/environment/envs)
- [部署方式](https://docs.cloudbase.net/run/deploy/deploy/introduce)

## 第三步：数据正式迁移

当出现以下任一情况，就应先迁移数据，再扩大流量：

- 有陌生网友开始持续投稿
- 需要多人同时访问
- 需要服务重启后保留数据
- 需要多个实例或自动扩容
- 活动数据已经有保存价值

迁移目标：

```text
提案、阶段、投票、进展
        -> CloudBase 数据库
静态资源             -> CloudBase 静态托管
复杂后端/API          -> CloudBase Run 或云函数
```

迁移时保留原来的 API 路径，前端不需要跟着大改：

- `GET /api/state`
- `GET /api/admin/state`
- `POST /api/proposals`
- `POST /api/votes`
- `POST /api/admin/proposals/review`
- `POST /api/admin/proposals/batch`
- `POST /api/admin/phase/status`
- `POST /api/admin/decision`

## 第四步：正式域名和备案

如果面向中国大陆长期公开，准备正式域名和 ICP 备案。顺序建议是：

1. 先用 CloudBase 默认地址做小范围测试。
2. 确认参与机制值得继续。
3. 购买并实名认证域名。
4. 按托管服务商流程办理 ICP 备案。
5. 绑定自定义域名和 HTTPS。
6. 准备隐私说明、用户协议、举报和删除机制。

腾讯云备案参考：

- [ICP 备案新手指引](https://cloud.tencent.com/document/product/243/45097)

## 正式上线前必须补的功能

按优先级：

1. 管理员正式登录，不再把共享密钥放在浏览器会话里。
2. 数据库读写和备份。
3. 提案举报、隐藏、审查记录。
4. 投票防刷和更可靠的参与者身份。
5. 内容安全审核。
6. 数据导出和定期备份。
7. 图片上传，再考虑音频和视频。
8. 多人协作者权限。

## 成本控制

第一轮不需要购买：

- 短信验证码
- 视频存储
- 实时 WebSocket
- 多实例扩容
- 独立搜索服务
- 复杂日志平台

先验证三件事：

```text
有人愿意提交
有人愿意投票
你能持续把结果做出来
```

只有这三件事成立，才值得增加云数据库、登录和媒体存储成本。
