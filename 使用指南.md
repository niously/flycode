# Flycode 使用指南

## 你现在拥有什么

一个可以在本机运行的 Flycode 0.1 版本，包含：

- ✅ 公开项目首页和提案提交
- ✅ 主持人管理工作台（审核、投票、决定、进展）
- ✅ 本地 JSON 数据库（提案、投票、阶段）
- ✅ 后端接口验证通过，可通过 `npm test` 重复执行冒烟测试
- ✅ 响应式设计（手机和桌面）

## 第一次启动

在终端执行：

```bash
cd C:/Users/l2104/flycode
npm start
```

看到 `Flycode is running at http://localhost:4173` 后，用浏览器打开：

```
http://localhost:4173
```

停止服务时按 `Ctrl + C`。

## 管理入口

点击页面右上角的"管理入口"，输入默认密钥：

```
flycode-local
```

这个密钥只适合本机体验，正式上线前必须更换。

### 更换管理密钥

启动时传入环境变量：

```bash
FLYCODE_ADMIN_KEY="你的随机长密钥" npm start
```

或者在 Windows PowerShell 里：

```powershell
$env:FLYCODE_ADMIN_KEY="你的随机长密钥"; npm start
```

## 数据在哪里

所有提案、投票和进展都保存在：

```
data/db.json
```

这个文件已经被 `.gitignore` 排除，不会意外提交到代码仓库。

**备份方式：**
1. 管理工作台里点"导出项目数据"
2. 或者直接复制 `data/db.json` 到安全位置

## 第一轮建议流程

1. 启动服务；本机使用 `http://localhost:4173`，同一 Wi-Fi 下的手机使用终端显示的网络地址
2. 邀请观众回答"你希望 Flycode 先做什么？"
3. 你在管理入口审核收到的提案（通过/不采用）
4. 审核几个有代表性的提案后，点"开启投票"
5. 观众投票（每个浏览器每轮一次）
6. 你公布一个决定，并写清楚接下来会做什么
7. 做出一个可见的改变，再用"发布进展"记录下来
8. 开启下一轮

## 下一步：从本地到正式上线

当前版本用本地 JSON 文件验证活动机制，只适合本机或局域网测试。确定有人持续参与后，建议按以下顺序升级：

### 0. 每次修改后的验收

服务运行时，在另一个终端执行：

```bash
npm test
```

它会临时创建提案，验证管理员权限、批量不采用、重新审查和永久删除，结束后自动清理测试数据。

### 1. 数据层迁移（优先）

**腾讯云 CloudBase 方案**（面向中国大陆用户）

- 把 `data/db.json` 的读写改成腾讯云数据库 API
- 前端静态文件部署到云开发静态托管
- 服务端逻辑改用云函数
- 好处：不需要自己维护服务器，访问路线可控

**参考文档：**
- [腾讯云开发静态网站托管](https://cloud.tencent.com/document/product/876/123943)
- [腾讯云开发价格](https://cloud.tencent.com/document/product/876/75213)

**注意事项：**
- 面向大陆用户需要办理 ICP 备案
- 先用临时域名小范围测试，再配置正式域名
- 备案文档：https://cloud.tencent.com/document/product/243/45097

### 2. 增强功能（按需）

**登录和身份**
- 微信登录（如果观众主要来自微信生态）
- 手机号验证码（需要短信服务，会产生费用）
- 暂时不做也可以，匿名参与足够第一轮

**图片上传**
- 允许网友上传图片说明想法
- 需要对象存储和 CDN
- 建议限制单张大小，在浏览器端压缩

**防刷和审核**
- 投票频率限制（当前已做 IP 级别）
- 内容审核 API（腾讯云内容安全）
- 举报和拉黑机制

**实时更新**
- WebSocket 推送新提案和投票结果
- 当前是 15 秒自动刷新，已经够用

### 3. 代码协作（未来）

如果有代码大佬加入，不需要在网站里重新造代码托管系统。建议分工：

- **Flycode 网站**：记录决策、展示进度、吸引参与者
- **Gitee 或 GitHub**：存放代码、Issue、Pull Request
- **视频平台**：传播和吸引新参与者

网站逐步增加：
- 项目代码仓库链接
- 开发任务列表
- 贡献者名单
- 技术方案讨论

## 技术栈说明

**当前版本（本地 MVP）：**
- Node.js 原生 HTTP 服务（无框架依赖）
- 原生 HTML + CSS + JavaScript（无前端框架）
- JSON 文件数据库

**为什么选这个栈：**
- 依赖最少，容易理解和修改
- 迁移到云服务时只需要改数据层
- 不会被前端框架版本绑死

**如果你以后想换：**
- 前端可以改成 Vue 或 React，接口保持不变
- 后端可以改成 Express 或 Koa，公开接口保持兼容
- 数据库可以换成 MongoDB 或 MySQL

## 目录结构

```
flycode/
├── server.js          # Node 服务（接口和静态文件）
├── package.json       # 项目配置
├── README.md          # 项目说明
├── .gitignore         # Git 忽略规则
├── data/
│   └── db.json        # 数据库（提案、投票、阶段）
└── public/            # 前端文件
    ├── index.html     # 页面结构
    ├── styles.css     # 样式
    └── app.js         # 交互逻辑
```

## 常见问题

**Q: 服务启动后，别人能通过 IP 访问吗？**

A: 可以在同一 Wi-Fi 下访问。电脑使用 `http://localhost:4173`，手机使用服务启动时显示的 `http://192.168.x.x:4173` 网络地址。当前版本仍只适合局域网测试，正式上线前需要部署到云服务器或云开发平台。

**Q: 数据会丢失吗？**

A: `data/db.json` 会持久保存。但建议定期备份，尤其是在修改代码前。

**Q: 如何重置数据，从空白开始？**

A: 删除 `data/db.json`，下次启动会自动创建初始数据。

**Q: 端口 4173 被占用怎么办？**

A: 修改 `server.js` 第 9 行的 `PORT`，或启动时传入环境变量：
```bash
PORT=8080 npm start
```

**Q: 管理密钥忘了怎么办？**

A: 本地版本默认是 `flycode-local`。如果你改过，重启时不传环境变量就会恢复默认。

## 需要帮助

如果遇到问题或想增加功能，可以：

1. 查看服务终端输出的错误信息
2. 打开浏览器开发者工具（F12）查看控制台
3. 检查 `data/db.json` 是否正常
4. 重启服务试试

## 许可和归属

这是你的项目。代码可以自由修改、部署和分享。建议在正式上线前：

- 准备用户协议和隐私政策
- 设置内容举报机制
- 考虑内容审核方案

## 下一步行动建议

**立即可做：**
1. 把本地链接发给几个信任的朋友，收集第一批反馈
2. 试走一遍完整流程：提交提案 → 审核 → 投票 → 公布决定
3. 确认手机上能正常使用

**活动开始前：**
1. 决定是先用本地测试，还是直接部署云服务
2. 准备视频内容和号召文案
3. 设定第一轮的截止时间

**第一轮结束后：**
1. 根据真实参与情况调整流程
2. 决定下一步要增加哪些功能
3. 考虑是否需要迁移到云服务

祝 Flycode 第一轮顺利！🚀
